/**
 * EditorialService contract (implemented by MockEditorialService in Phase 1):
 * getState, getActiveIssue, listCourses, listCourseWork(courseId), createIssue,
 * setActiveIssue, listArticles(filters, session), saveArticleEdit,
 * updateArticleStatus, listPhotos(filters, session), submitPhotos,
 * updatePhotoStatus, publishIssue.
 *
 * Production implementations call a private backend. That backend validates the
 * secure session and Classroom membership for every request; the frontend never
 * supplies a trusted role or studentId.
 */
export class EditorialService {}
