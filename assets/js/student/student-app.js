import { requireRole } from '../auth/route-guard.js';
import { editorialService } from '../services/service-container.js';
import { initShell } from '../shared/shell.js';
import { emptyState, escapeHtml, formatDate, statusBadge, toast } from '../shared/ui.js';
const session=requireRole('student'); if(!session) throw new Error('Redirecting'); initShell(session,'My Page');
let selectedArticle=null; const state=()=>editorialService.getState();
const typeLabel=(article,s)=>s.issues.find(i=>i.id===article.issueId)?.articleTypes.find(t=>t.id===article.articleTypeId)?.label||'Article';
function articleCard(a,s){const edited=Boolean(a.editedContent);return `<article class="panel article-card"><div class="editor-meta"><span class="eyebrow">${escapeHtml(typeLabel(a,s))}</span>${statusBadge(a.status)}</div><h2>${escapeHtml(a.title)}</h2><div class="card-meta"><span>Submitted ${formatDate(a.submittedAt)}</span><span>Original submitted ✓</span><span>Edited version ${edited?'available ✓':'not available'}</span></div><div class="card-actions"><button data-open-article="${a.id}" data-version="original">View original</button><button data-open-article="${a.id}" data-version="edited" ${edited?'':'disabled'}>View edited version</button></div></article>`}
function render(){const s=state(),issue=s.issues.find(i=>i.status==='active'),articles=editorialService.listArticles(issue?{issueId:issue.id}:{},session),photos=editorialService.listPhotos(issue?{issueId:issue.id}:{},session);const head=`<header class="page-head"><div><p class="eyebrow">The Lion's Pride</p><h1>My Page</h1><p>Welcome, ${escapeHtml(session.name)}.</p></div><strong class="issue-label">${escapeHtml(issue?.name||'No active issue')}</strong></header>`;document.querySelector('[data-view=dashboard]').innerHTML=head+(issue?`<div class="stats-grid grid"><div class="panel stat"><strong>${articles.length}</strong><span>Article submissions</span></div><div class="panel stat"><strong>${articles.filter(a=>a.editedContent).length}</strong><span>Edited versions</span></div><div class="panel stat"><strong>${photos.length}</strong><span>Photos submitted</span></div></div><h2 class="section-title">My articles</h2><div class="article-cards grid" style="margin-top:18px">${articles.length?articles.map(a=>articleCard(a,s)).join(''):emptyState('No submissions yet','Selected Classroom assignments will appear here after submission.')}</div>`:`<div class="notice notice--error">No active Issue is configured. Ask an editor to activate an Issue.</div>`);document.querySelector('[data-view=articles]').innerHTML=head+`<h2 class="section-title">All article submissions</h2><div class="article-cards grid" style="margin-top:18px">${articles.length?articles.map(a=>articleCard(a,s)).join(''):emptyState('No submissions yet','There are no submissions linked to your verified account.')}</div>`;document.querySelector('[data-view=photos]').innerHTML=head+`<h2 class="section-title">Photo submissions</h2><div class="photo-grid grid" style="margin-top:18px">${photos.length?photos.map(p=>`<article class="panel photo-card"><div class="photo-thumb" style="--photo-color:${p.color}">METADATA PREVIEW</div><div class="photo-info"><div class="editor-meta"><strong>${escapeHtml(p.filename)}</strong>${statusBadge(p.status)}</div><p>${escapeHtml(p.caption)}</p><small>${formatDate(p.createdAt)}</small></div></article>`).join(''):emptyState('No photos submitted','Use Upload Photos to attach files to one of your articles.')}</div>`;const select=document.querySelector('[name=articleSubmissionId]');select.innerHTML=articles.map(a=>`<option value="${a.id}">${escapeHtml(typeLabel(a,s))} — ${escapeHtml(a.title)}</option>`).join('');document.querySelector('[data-upload-form] button.btn-primary').disabled=!articles.length;bindDynamic();}
function bindDynamic(){document.querySelectorAll('[data-open-article]').forEach(b=>b.onclick=()=>openArticle(b.dataset.openArticle,b.dataset.version));}
function openArticle(id,version){const s=state();selectedArticle=s.articles.find(a=>a.id===id);document.querySelector('#article-modal-title').textContent=selectedArticle.title;document.querySelector('[data-article-modal]').classList.add('is-open');showVersion(version);}
function showVersion(version){const content=version==='edited'?selectedArticle.editedContent:selectedArticle.originalContent;document.querySelector('[data-article-content]').innerHTML=content||emptyState('Not available','An editor has not shared an edited version yet.');}
document.querySelectorAll('[data-student-view]').forEach(b=>b.onclick=()=>{document.querySelectorAll('.view').forEach(v=>v.hidden=v.dataset.view!==b.dataset.studentView);document.querySelectorAll('[data-student-view]').forEach(x=>x.classList.toggle('is-active',x===b));document.querySelector('.app-sidebar').classList.remove('is-open');});document.querySelectorAll('[data-open-upload]').forEach(b=>b.onclick=()=>document.querySelector('[data-upload-modal]').classList.add('is-open'));document.querySelectorAll('[data-close-modal]').forEach(b=>b.onclick=()=>b.closest('.modal').classList.remove('is-open'));document.querySelectorAll('[data-version]').forEach(b=>b.onclick=()=>showVersion(b.dataset.version));document.querySelector('[data-upload-form]').onsubmit=e=>{e.preventDefault();const form=e.currentTarget,fd=new FormData(form),files=form.elements.files.files;if(!files.length)return;try{const issue=editorialService.getActiveIssue();editorialService.submitPhotos({issueId:issue.id,articleSubmissionId:fd.get('articleSubmissionId'),caption:fd.get('caption'),photographer:fd.get('photographer'),sourceType:fd.get('sourceType')},files,session);form.reset();document.querySelector('[data-upload-modal]').classList.remove('is-open');render();toast(`${files.length} photo file${files.length===1?'':'s'} queued in mock storage.`);}catch(err){toast(err.message,'error');}};render();
